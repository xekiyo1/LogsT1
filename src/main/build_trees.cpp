#include <bits/stdc++.h>
using namespace std;




/**
 * Este archivo (Accesado a través del comando del CLI)
 * se ocupa de armar árboles R según especificaciones
 */
int main(int argc, char** argv){
	int expected_arguments = 2;
	string expected_usage = (string) argv[0] + " [N (2^n blocks will be used)] [dataset]";

	if (argc != expected_arguments + 1){
		cout << "Usage: " << expected_usage << endl;
		return 0;
	}

	/*
	  Acá irá la lógica detrás de crear un arbol R de tamaño 2^N con el dataset bin/data/x.bin
	*/



	return 0;
}